{
  "mcpServers": {
    "napari-mcp": {
      "command": "/opt/conda/envs/napari-mcp/bin/napari-mcp",
      "transport": "stdio",
      "description": "In-container NapariMCP standalone server. Runs over stdio in its own conda env; the napari Viewer is created lazily (only on the first napari tool call), so nothing heavy runs until the agent uses it."
    }
  }
}
